<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>ignition-kafka</artifactId>
        <groupId>com.freeno83.ignition.kafka</groupId>
        <version>${revision}</version>
    </parent>

    <artifactId>build</artifactId>

    <properties>
        <maven.build.timestamp.format>yyyyMMddHH</maven.build.timestamp.format>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.freeno83.ignition.kafka</groupId>
            <artifactId>client</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.freeno83.ignition.kafka</groupId>
            <artifactId>common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.freeno83.ignition.kafka</groupId>
            <artifactId>designer</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.freeno83.ignition.kafka</groupId>
            <artifactId>gateway</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>com.inductiveautomation.ignitionsdk</groupId>
                <artifactId>ignition-maven-plugin</artifactId>
                <version>1.1.0</version>

                <executions>
                    <execution>
                        <id>package-modl</id>
                        <phase>package</phase>
                        <goals>
                            <goal>modl</goal>
                        </goals>
                    </execution>
                </executions>

                <configuration>
                    <projectScopes>
                        <projectScope>
                            <name>client</name>
                            <scope>C</scope>
                        </projectScope>
                        <projectScope>
                            <name>common</name>
                            <scope>CDG</scope>
                        </projectScope>
                        <projectScope>
                            <name>designer</name>
                            <scope>CD</scope>
                        </projectScope>
                        <projectScope>
                            <name>gateway</name>
                            <scope>G</scope>
                        </projectScope>
                    </projectScopes>

                    <moduleId>${project.groupId}</moduleId>
                    <moduleName>${module-name}</moduleName>
                    <moduleDescription>${module-description}</moduleDescription>
                    <moduleVersion>${revision}.${maven.build.timestamp}</moduleVersion>
                    <requiredIgnitionVersion>${ignition-platform-version}</requiredIgnitionVersion>

                    <depends>
                        <depend>
                            <scope>G</scope>
                            <moduleId>com.inductiveautomation.opcua</moduleId>
                        </depend>
                    </depends>

                    <hooks>
                        <hook>
                            <scope>C</scope>
                            <hookClass>com.freeno83.ignition.kafka.ClientHook</hookClass>
                        </hook>
                        <hook>
                            <scope>D</scope>
                            <hookClass>com.freeno83.ignition.kafka.DesignerHook</hookClass>
                        </hook>
                        <hook>
                            <scope>G</scope>
                            <hookClass>com.freeno83.ignition.kafka.GatewayHook</hookClass>
                        </hook>
                    </hooks>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
